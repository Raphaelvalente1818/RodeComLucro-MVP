{
  "name": "@rode/calc",
  "version": "0.1.0",
  "private": true,
  "description": "Motor de cálculo de frete do RODE COM LUCRO — pacote isomórfico (PWA + Edge Functions Deno). Extraído e adaptado de github.com/emerson1001a/calculadora-experimental, validado em campo por um motorista real.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vitest": "^2.0.0"
  }
}
